interface:
  display_name: "Dev Flow"
  short_description: "Primary designs, delegates coding, and verifies results"
  default_prompt: "Use $dev-flow to design and verify this feature while delegating only bounded implementation work."

policy:
  allow_implicit_invocation: false
